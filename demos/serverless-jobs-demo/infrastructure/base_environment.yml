name: serverless_base_environment_demo
channels:
  - conda-forge
  - defaults
dependencies:
  # Base Python version
  - python=3.10
  
  # Pip-installed packages from requirements.txt
  - pip
  - pip:
    # Data Processing
    - polars>=0.19.0
    - duckdb>=0.9.0
    - pyarrow>=14.0.0
    
    # Data Validation
    - great-expectations>=0.18.0
    - pandera>=0.17.0
    
    # Healthcare Data Standards
    - hl7apy>=1.3.4
    - fhir.resources>=7.0.0
    
    # Machine Learning & Statistics
    - scikit-learn>=1.3.0
    - statsmodels>=0.14.0
    - imbalanced-learn>=0.11.0
    
    # Utilities
    - tqdm>=4.66.0
    - python-dotenv>=1.0.0
    
    # Testing
    - pytest>=7.4.0
    - pytest-cov>=4.1.0
    
    # Custom wheel (if accessible from workspace)
    # Note: You may need to upload this to a workspace path or volume
    # - /Volumes/maggiedatabricksterraform_dbw/synthea/admin_configs/hls_external_libs-0.1.0-py3-none-any.whl

env:
  - name: keyvault
    value: 'prod'

